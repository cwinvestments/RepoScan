.claude/
.mcp.json
__pycache__/
*.pyc

# Agent Bridge (auto-added by let-them-talk init)
.agent-bridge/
.codex/
.gemini/

# Regression test artifacts (transient, reviewable locally)
regression_*/

# SQLite runtime files (UI creates scans.db from schema.sql on first run)
scans.db
scans.db-journal
scans.db-wal
scans.db-shm
scans_smoketest.db
scans_postfix_smoketest.db
